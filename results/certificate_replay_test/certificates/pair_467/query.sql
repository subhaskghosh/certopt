SELECT
  name AS CUSTOMERS
FROM (
  SELECT
    CUSTOMERS.NAME,
    ORDERS.ID AS ORDERID
  FROM CUSTOMERS
  LEFT JOIN ORDERS
    ON CUSTOMERS.ID = ORDERS.CUSTOMERID
) AS CUSTOMERSORDERS
WHERE
  orderid IS NULL