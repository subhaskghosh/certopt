SELECT
  email AS EMAIL
FROM (
  SELECT
    EMAIL,
    DENSE_RANK() OVER (PARTITION BY NULL ORDER BY EMAIL ASC NULLS LAST) AS RANK
  FROM PERSON
) AS TEMP
GROUP BY
  temp.rank
HAVING
  COUNT(email) > 1