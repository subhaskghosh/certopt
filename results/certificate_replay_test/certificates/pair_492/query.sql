SELECT
  aa.name AS CUSTOMERS
FROM (
  SELECT
    C.ID,
    NAME,
    O.ID AS NUM,
    CUSTOMERID
  FROM CUSTOMERS AS C
  LEFT JOIN ORDERS AS O
    ON C.ID = O.CUSTOMERID
) AS AA
WHERE
  aa.num IS NULL