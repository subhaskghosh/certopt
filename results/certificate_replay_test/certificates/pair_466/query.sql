SELECT
  t1.name AS CUSTOMERS
FROM CUSTOMERS AS T1
LEFT JOIN (
  SELECT
    T1.ID,
    T1.NAME
  FROM CUSTOMERS AS T1
  JOIN ORDERS AS T2
    ON T1.ID = T2.CUSTOMERID
) AS T2
  ON t1.id = t2.id
WHERE
  t2.id IS NULL