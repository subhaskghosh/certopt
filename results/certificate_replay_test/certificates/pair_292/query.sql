SELECT DISTINCT
  a.email AS EMAIL
FROM PERSON AS A
CROSS JOIN (
  SELECT
    EMAIL,
    COUNT(EMAIL) AS CNT
  FROM PERSON
  GROUP BY
    EMAIL
  HAVING
    COUNT(EMAIL) > 1
) AS TMP
WHERE
  a.email = tmp.email