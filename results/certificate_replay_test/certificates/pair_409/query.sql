SELECT
  name AS CUSTOMERS
FROM (
  SELECT
    CUSTOMERS.NAME,
    ORDERS.CUSTOMERID
  FROM CUSTOMERS
  LEFT JOIN ORDERS
    ON CUSTOMERS.ID = ORDERS.CUSTOMERID
) AS TEMP
WHERE
  temp.customerid IS NULL