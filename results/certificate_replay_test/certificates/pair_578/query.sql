SELECT
  c1.name AS CUSTOMERS
FROM CUSTOMERS AS C1
WHERE
  NOT C1.ID IN (
    SELECT
      C2.ID
    FROM CUSTOMERS AS C2
    CROSS JOIN ORDERS AS O2
    WHERE
      C2.ID = O2.CUSTOMERID
  )
GROUP BY
  c1.id,
  c1.name