SELECT
  t.name AS CUSTOMERS
FROM (
  SELECT
    NAME,
    COUNT(O.ID) AS FO
  FROM CUSTOMERS AS C
  LEFT JOIN ORDERS AS O
    ON C.ID = O.CUSTOMERID
  GROUP BY
    C.ID
  HAVING
    COUNT(O.ID) = 0
) AS T