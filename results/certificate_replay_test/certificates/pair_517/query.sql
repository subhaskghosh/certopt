SELECT
  customers
FROM (
  SELECT
    NAME AS CUSTOMERS,
    CUSTOMERS.ID AS ID1,
    ORDERS.ID AS ID2,
    CUSTOMERID
  FROM CUSTOMERS
  LEFT JOIN ORDERS
    ON CUSTOMERS.ID = ORDERS.CUSTOMERID
) AS CC
WHERE
  customerid IS NULL