SELECT
  firstname,
  lastname,
  (
    SELECT
      CITY
    FROM ADDRESS
    WHERE
      PERSON.PERSONID = ADDRESS.PERSONID
  ) AS CITY,
  (
    SELECT
      STATE
    FROM ADDRESS
    WHERE
      PERSON.PERSONID = ADDRESS.PERSONID
  ) AS STATE
FROM PERSON