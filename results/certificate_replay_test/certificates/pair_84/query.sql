SELECT
  p.firstname,
  p.lastname,
  CASE WHEN Q.CITY IS NULL THEN NULL ELSE Q.CITY END AS CITY,
  CASE WHEN Q.STATE IS NULL THEN NULL ELSE Q.STATE END AS STATE
FROM PERSON AS P
LEFT JOIN ADDRESS AS Q
  ON p.personid = q.personid